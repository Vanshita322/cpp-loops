#include<iostream>
using namespace std;
int main() {
    int a,b;
    cin>>a>>b;
    int gcd,lcm;
    int i;
    for(i=1;i<=min(a,b);i++) {
        if((a%i==0) && (b%i==0)) {
            gcd = i;
        }
    }
    lcm = (a*b)/gcd;
    cout<<gcd<<endl;
    cout<<lcm<<endl;
    return 0;
}
