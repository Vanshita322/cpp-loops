#include<iostream>
using namespace std;
int main() {
    int num;
    cin>>num;
    int digit;
    int sum = 0;
    while(num>0) {
        digit = num % 10;
        sum = sum + digit;
        num = num / 10;  // This line was missing
    }
    cout<<"Required Sum is : "<<sum;
    return 0;
}
