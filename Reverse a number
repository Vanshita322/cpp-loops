#include<iostream>
using namespace std;
int main() {
    int num;
    cin>>num;
    int digit;
    int rev = 0;
    while(num>0) {
        digit = num % 10;
        rev = rev * 10 + digit;
        num/=10;
    }
    cout<<"Reveresed no is : "<<rev;
    return 0;
}
